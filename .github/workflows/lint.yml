name: Lint

on:
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-24.04

    steps:
    - uses: actions/checkout@v7

    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version: "3.12"
    - run: pip install tox

    - name: Lint the code
      run: tox -e lint
